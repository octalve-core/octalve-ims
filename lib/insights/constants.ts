/**
 * REQ-0085 — shared catalog/warehouse insight thresholds (client + server safe).
 * Aligned with BusinessInsightPage low-stock rule (qty ≤ 20).
 */

export const CATALOG_LOW_STOCK_THRESHOLD = 20;
